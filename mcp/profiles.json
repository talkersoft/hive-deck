{
  "profiles": {
    "decks": {
      "tools": [
        "status", "list", "decks",
        "init", "ship", "sync", "default",
        "stash", "unstash", "list-pulls",
        "teardown", "prune", "mcp", "await-merge",
        "repo-list", "repo-add"
      ]
    },
    "workflows": {
      "extends": "decks",
      "tools": ["promote", "workflow-run", "workflow-plan", "workflow-list"]
    }
  }
}
